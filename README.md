# IRSProject
A project of Information Retrieval Systems based on the Fuzzy Boolean Model

## UI Modes

- Terminal (default): `python main.py`
- GUI: `python main.py --ui gui`
- GUI with env var (PowerShell): `$env:IRS_UI='gui'; python main.py`
